import React from "react";
import { AntDesign } from "@expo/vector-icons";

import { COLORS } from "../../../../global/colors";

export default function PlusIcon() {
  return <AntDesign name="plus" size={24} color={COLORS.text} />;
}
