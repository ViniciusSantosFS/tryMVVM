import React from "react";
import { AntDesign } from "@expo/vector-icons";

import { COLORS } from "../../../../global/colors";

export default function Calendar() {
  return <AntDesign name="calendar" size={16} color={COLORS.warn} />;
}
